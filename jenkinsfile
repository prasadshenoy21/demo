pipeline {    agent any
    stages {        stage('Build') {            steps {                sh 'mvn clean package'            }   post{    success{     echo ' archiving'     archiveArtifacts artifacts: '**/*.war    }   }        }        stage('Test') {            steps {                echo 'Testing..'            }        }        stage('Deploy') {            steps {                build job: 'deploy-to-stagging'            }        }    }}
